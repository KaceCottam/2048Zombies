#ifndef PLAYER_INPUT_HPP
#define PLAYER_INPUT_HPP

enum class DirectionInputType : int { UP = -1, DOWN = 1, LEFT = 0, RIGHT = 2 };

enum class MenuInputType { PLAY, EXIT };

#endif  // ! PLAYER_INPUT_HPP
